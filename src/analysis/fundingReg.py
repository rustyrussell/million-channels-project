from matplotlib import pyplot as plt
from scipy import optimize


def fundingRegExperiment(channels):
    """
    Find if there is a correlation between # of channels and total value
    :param channels:
    :return:
    """

    # run funding
